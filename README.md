# DLV-Bash
Bash script to make using yt-dlp easier. 
Also removes the tracking part of a Youtube link.

• Requires yt-dlp to function.
• Set the directory path to whatever folder you're using for videos.
